"use strict";

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Column already exists, skipping
  },

  down: async (queryInterface, Sequelize) => {
    // Nothing to remove
  },
};
